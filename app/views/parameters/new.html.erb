<%= form_for [@process_step, @parameter], method: :post, local: true do |f| %>
  <%= f.label :measurement, "Measurement: " %>
  <%= f.number_field :measurement, step: :any %>

  <% f.number_field :process_step_id, value: @process_step_id %>
  <%= f.submit 'Submit' %>
<% end %>
