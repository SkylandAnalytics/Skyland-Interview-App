<ul>
  <% @process_steps.each do |process_step| %>
    <li><%= link_to process_step.name, process_step_path(process_step)  %></li>
    <li><%= process_step.description %></li>
    <li><%= process_step.position %></li>
  <% end %>
</ul>

